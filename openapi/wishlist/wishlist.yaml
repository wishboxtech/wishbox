post:
  tags:
    - "Wishlist"
  summary: "Create Wishlist - NOT READY"
  description:
  consumes:
    - "application/json"
  produces:
    - "application/json"
  security:
    - ApiKeyAuth: []
  parameters:
    - name: "profileid"
      in: "path"
      description: "user profile id"
      required: True
      type: "string"
    - in: "body"
      name: "body"
      description: "Create Wishlist"
      required: true
      schema:
        $ref: "./definitions.yaml#/CreateWishlist"
  responses:
    201:
      description: "successful operation"
      schema:
        type: "object"
        properties:
          ok:
            type: "boolean"
            default: true
          data:
            type: "object"
            properties:
              done:
                type: "string"
          status:
            type: "number"
            default: 200
    400:
      description: "Bad Request"
    401:
      description: "Unauthorized"
    403:
      description: "forbidden"

patch:
  tags:
    - "Wishlist"
  summary: "Edit Wishlist - NOT READY"
  description:
  consumes:
    - "application/json"
  produces:
    - "application/json"
  security:
    - ApiKeyAuth: []
  parameters:
    - name: "profileid"
      in: "path"
      description: "user profile id"
      required: True
      type: "string"
    - in: "body"
      name: "body"
      description: "Edit Wishlist"
      required: true
      schema:
        $ref: "./definitions.yaml#/EditWishlist"
  responses:
    201:
      description: "successful operation"
      schema:
        type: "object"
        properties:
          ok:
            type: "boolean"
            default: true
          data:
            type: "object"
            properties:
              done:
                type: "string"
          status:
            type: "number"
            default: 200
    400:
      description: "Bad Request"
    401:
      description: "Unauthorized"
    403:
      description: "forbidden"


get:
  tags:
    - "Wishlist"
  summary: "Get User Wishlists - NOT READY"
  description:
  consumes:
    - "application/json"
  produces:
    - "application/json"
  security:
    - ApiKeyAuth: []
  parameters:
    - name: "profileid"
      in: "path"
      description: "user profile id"
      required: True
      type: "string"
  responses:
    200:
      description: "successful operation"
      schema:
        type: "object"
        properties:
          ok:
            type: "boolean"
          data:
            $ref: "./definitions.yaml#/Wishlists"
          status:
            type: "number"
            default: 201
    400:
      description: "Bad Request"
    429:
      description: "Throttled"