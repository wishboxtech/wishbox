post:
  tags:
    - "Wishlist"
  summary: "Create Wish - NOT READY"
  description:
  consumes:
    - "application/json"
  produces:
    - "application/json"
  security:
    - ApiKeyAuth: []
  parameters:
    - name: "profileid"
      in: "path"
      description: "user profile id"
      required: True
      type: "string"
    - in: "body"
      name: "body"
      description: "Create Wishlist"
      required: true
      schema:
        $ref: "./definitions.yaml#/CreateWish"
  responses:
    200:
      description: "successful operation"
      schema:
        type: "object"
        properties:
          ok:
            type: "boolean"
            default: true
          data:
            $ref: "./definitions.yaml#/Wish"
          status:
            type: "number"
            default: 200

patch:
  tags:
    - "Wishlist"
  summary: "Edit Wish - NOT READY"
  description:
  consumes:
    - "application/json"
  produces:
    - "application/json"
  security:
    - ApiKeyAuth: []
  parameters:
    - name: "profileid"
      in: "path"
      description: "user profile id"
      required: True
      type: "string"
    - in: "body"
      name: "body"
      description: "Edit Wishlist"
      required: true
      schema:
        $ref: "./definitions.yaml#/EditWish"
  responses:
    200:
      description: "successful operation"
      schema:
        type: "object"
        properties:
          ok:
            type: "boolean"
            default: true
          data:
            $ref: "./definitions.yaml#/Wish"
          status:
            type: "number"
            default: 200

get:
  tags:
    - "Wishlist"
  summary: "Get Wishes - NOT READY"
  description:
  consumes:
    - "application/json"
  produces:
    - "application/json"
  security:
    - ApiKeyAuth: []
  parameters:
    - name: "profileid"
      in: "path"
      description: "user profile id"
      required: True
      type: "string"
  responses:
    200:
      description: "successful operation"
      schema:
        type: "object"
        properties:
          ok:
            type: "boolean"
            default: true
          data:
            $ref: "./definitions.yaml#/Wishes"
          status:
            type: "number"
            default: 200