patch:
  tags:
    - "Wishlist"
  summary: "Edit Wish - SEMI READY"
  description: edit a wish
  consumes:
    - "application/json"
  produces:
    - "application/json"
  security:
    - ApiKeyAuth: []
  parameters:
    - name: "id"
      in: "path"
      description: "user wishlist id"
      required: True
      type: "string"
    - name: "wishid"
      in: "path"
      description: "user wish id"
      required: True
      type: "string"
    - in: "body"
      name: "body"
      description: "Create Wishlist"
      required: true
      schema:
        $ref: "./definitions.yaml#/EditWish"
  responses:
    200:
      description: "successful operation"
      schema:
        type: "object"
        properties:
          ok:
            type: "boolean"
            default: true
          data:
            $ref: "./definitions.yaml#/Wishes"
          status:
            type: "number"
            default: 200

delete:
  tags:
    - "Wishlist"
  summary: "Delete Wish - READY"
  description: delete a wish
  consumes:
    - "application/json"
  produces:
    - "application/json"
  security:
    - ApiKeyAuth: []
  parameters:
    - name: "id"
      in: "path"
      description: "user wishlist id"
      required: True
      type: "string"
    - name: "wishid"
      in: "path"
      description: "user wish id"
      required: True
      type: "string"
  responses:
    200:
      description: "successful operation"
      schema:
        type: "object"
        properties:
          ok:
            type: "boolean"
            default: true
          data:
            $ref: "./definitions.yaml#/Wishes"
          status:
            type: "number"
            default: 200